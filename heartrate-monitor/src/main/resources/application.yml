server:
  port: 8082
  simulatehrm: true
spring:
  main:
    web-application-type: reactive
  application:
    name: heartrate-monitor-service
  rabbitmq:
    port: 5672
    host: 127.0.0.1
    username: guest
    password: guest
#    virtual-host: vhost

  jackson:
    serialization:
      write_dates_as_timestamps: false
